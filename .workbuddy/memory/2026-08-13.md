# 2026-08-13

## 版本号变更

按用户要求将鸿蒙版本号从 `1.0.0` 升级到 `1.2.0.1`：

- `apps/harmony/AppScope/app.json5`：`versionCode` 3 → 4（严格递增，上架要求）、`versionName` "1.0.0" → "1.2.0.1"
- `apps/harmony/products/entry/src/main/ets/pages/SettingsPage.ets`：第 34 行 `@State version: string` 默认值 '1.0.0' → '1.2.0.1'（设置页底部 `AudioDock HarmonyOS v${this.version}` 文案）

未动 mobile 端，也无 CHANGELOG/RELEASE 文件需要同步。
build/ 目录里的 module.json 是 hvigor 自动生成的，无需手动改。

## BundleInfo 工具链落点

新增/修改用于支持鸿蒙端 `bundleManager.getBundleInfoForSelf` 调用：

- 新增 `apps/harmony/common/audiodock_common/src/main/ets/utils/bundleInfo.ets`（4 个工具函数：`getSelfBundleInfo` / `getSelfAppVersion` / `getSelfSignatureInfo` / `getSelfRequestedPermissions`）
- 导出同步到 `common/audiodock_common/Index.ets`
- entry 模块 `products/entry/src/main/ets/entryability/EntryAbility.ets`：
  - import 增加 `getSelfBundleInfo, getSelfAppVersion`
  - `onCreate` 末尾（`loadPage('pages/Index')` 之前）fire-and-forget 调用 `this.dumpSelfBundleInfo()`，不阻塞冷启动
  - 新增私有方法 `dumpSelfBundleInfo()`：并行两条日志（versionName/versionCode + 全量 summary）
- 新增本地 mock `apps/harmony/scripts/mock-bundle-info.mjs`（字段结构 1:1 对齐 @kit.AbilityKit，但仅作字段预览，不可替代真机）。Node 22 跑通。

待用户决策：是否把以上探测封装进调试 Modal 页面（`pages/BundleInfoDebugPage.ets`），还是直接接入业务 Modal。
