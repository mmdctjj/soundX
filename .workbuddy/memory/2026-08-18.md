# 2026-08-18

## hm 登录页协议勾选
- `apps/harmony/products/entry/src/main/ets/pages/MemberLoginPage.ets`：
  - 协议行加 Checkbox（`.shape(CheckBoxShape.ROUNDED_SQUARE)` 显示为方形复选框），文案「登录即代表同意」→「同意」。
  - `handleLogin` 增加未勾选拦截：toast `memberLogin.mustAgree` + 协议行左右摇摆动画（`agreeShakeX` translate + 45ms 分段 setTimeout，衰减序列 ±10/±8/±5）。
  - 整行 onClick 也可切换勾选（两个链接 Text 自身 onClick 优先）。
- i18n：`features/i18n/src/main/ets/locales/zh-CN.ts`、`en.ts` 改 `loginAgree`，新增 `mustAgree`。
- 备注：本机无 hvigorw 命令行，编译验证需在 DevEco Studio 进行。
- ArkUI 踩坑：带文字参数的 `Button(label)` / `Button(label, {type})` 形式圆角会被按钮自身样式接管（Normal 变直角、Capsule 变胶囊）；**容器形式 `Button() { Text(...) }` + 链式 `.borderRadius(n)` 才稳定生效**（项目里登录按钮就是这种写法）。获取验证码按钮已从 label 形式改为容器形式。

## hm 支付入口隐藏（soft-disable）【已撤销】
- 曾将 7 处跳转 MemberBenefitsPage 的入口注释下线，后用户决定：未开通 VIP 不拦截、正常进会员权益页，已全部恢复原状；`common.vipOnly` 文案也还原为「仅限会员使用」。
- 教训：这类"隐藏入口"需求先确认非 VIP 的引导链路是否保留再动手；恢复时把 i18n 文案等连带改动一并还原。

## hm 支付按钮隐藏（当前生效）
- MemberBenefitsPage.ets：「支付方式」标题 + 微信支付按钮整段包在 `if (false) { ... }` 里（标记 `[支付按钮已隐藏]`），恢复时改成 `if (true)`。
- 入口跳转不拦截（用户可正常进会员权益页浏览套餐），只是页面上看不到支付按钮。
- hm 端只有微信支付一种支付方式（handlePayment 固定 'WECHAT'），无支付宝按钮。
- 不退款提示、联系客服、退出登录按钮保留显示。

## hm MiniPlayer 空队列不占高度
- MiniPlayer.ets 本身空队列时就不渲染内容，但各页面列表底部固定 `padding bottom: 116`（MiniPlayer 60 + Tab 56）会留出空白。
- MiniPlayer.ets 新增导出 `miniPlayerHasContent()`（队列非空判断）；7 处页面 padding 改为 `miniPlayerHasContent() ? 116 : 56`：
  MainPage、SearchPage ×2、AlbumDetailPage、ArtistDetailPage、PlaylistDetailPage、CollectionDetailPage。
- 局限：非响应式，播放状态变化时在当前页面内 padding 不刷新，页面重建后生效（可接受）。
