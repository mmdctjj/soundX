# 2026-08-09 工作日志

## 今日要事：AudioDock Mobile 版本检测 + 跨平台商店跳转（plan 完成）

### 完成
- 在 `feat/test` 分支上完成「跨平台版本检测 + 应用商店跳转」完整方案设计
- 计划已写入 `/Users/ctjj/.workbuddy/plans/stellar-nebula-newton.md`
- 用户已批准，等用户提供各商店具体 URL 后即可开工

### 关键探索发现
- 旧版版本检查完整实现（`useCheckUpdate.ts` 145行 + `UpdateModal.tsx` 204行 + 原生模块 `SystemDownloadManager` + 92行 `updateUtils.ts`）已于 commit `f7d5fa28`（2026-07-31）整体删除
- 剩余 `updateUtils.ts` 只剩 9 行 `getLocalVersion()`
- `product-updates.tsx` 仍用 GitHub Releases 拉 changelog（但只展示，无跳转/下载）
- 后端接口 `GET /download/latest?product=audiodock` 通过 `plusRequest` 调用（`packages/services/src/plus.ts`）
- i18n `update.*` 翻译键（zh-CN + en）仍存在，可直接复用

### 用户决策记录
- iOS: App Store（URL 待补）
- Android: 华为 AppGallery + 小米 + OPPO + vivo（URL 待补）
- HarmonyOS: 暂不做
- 商店 URL 配置位置: `src/constants/store.ts` 客户端常量
- 数据源: 后端 `plusRequest.get('/download/latest')`
- 触发时机: App 启动静默 + 设置页手动
- v1 范围: 仅「提示 + 跳转」，不做强制/可选分级
- APK 直装: 彻底丢弃，不保留兜底

### 项目约定（新增）
- 平台判断统一封装到 `src/utils/platform.ts`（未来加 `isHarmony` 留接口）
- 打开外部链接统一封装到 `src/utils/openURL.ts`（Linking.canOpenURL + openURL + WebBrowser fallback）
- 业务常量（store URL）放 `src/constants/store.ts`，不用 `.env`（项目目前没引入 env 体系）

### 实施顺序（待用户给 URL 后）
1. commit 1: `feat(mobile): add cross-platform store constants and platform utils`
2. commit 2: `feat(mobile): restore useCheckUpdate hook with backend API`
3. commit 3: `feat(mobile): add UpdateModal and wire into launch + settings`

### 实施完成
- 2026-08-09 11:57 用户提供了 iOS URL: `https://apps.apple.com/cn/app/audiodock/id6761128589`，其余 Android 用 PLACEHOLDER 占位
- 3 个 commit 全部完成在 feat/test 分支：
  - `931064c1` commit 1: constants + platform + openURL（129 行）
  - `0f338786` commit 2: services + hook + updateUtils 扩展 + i18n 新增 9 个 key（418 行）
  - `f0529f1d` commit 3: UpdateModal + _layout 接线 + settings 按钮（271 行）
- 类型检查全部通过（0 errors）
- 清理验证：`git grep SystemDownloadManager` / `downloadAndInstallApk(` 均无结果
- i18e dist 重新构建以让新 key 运行时生效

### 后续待办
- Android 4 家商店 PLACEHOLDER 待用户填真实 URL
- 后端 `/download/latest` 接口需要确认还活着；如果挂了，service 层已静默 catch 不弹窗
- iOS 若仅 TestFlight，需要把 URL 换成 `https://testflight.apple.com/join/xxxxx`