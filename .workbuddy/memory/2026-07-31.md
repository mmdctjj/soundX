# 2026-07-31

## 首次启动隐私协议弹窗（feat/test 分支）

- 需求范围（经用户两次确认）：仅 **mobile 的 Android 平台** 实现，iOS / mini / harmony 均不做。
- 新增 `apps/mobile/src/components/PrivacyAgreementDialog.tsx`：
  - 标题"欢迎使用 声仓"，正文含《用户协议》《隐私政策》蓝色（#1677ff）可点链接，链接地址取自会员登录页 `app/member-login.tsx`：
    - 用户协议：https://www.audiodock.cn/docs/user-agreement/
    - 隐私政策：https://www.audiodock.cn/docs/privacy-policy/
  - 首次启动检测：AsyncStorage key `privacy_agreement_accepted`，未同意则弹窗；读取失败时兜底也弹窗。
  - "同意并继续"：写入标记并关闭弹窗；"不同意"：`BackHandler.exitApp()` 退出 App。
  - `onRequestClose` 拦截返回键，防止绕过选择；仅 `Platform.OS === "android"` 渲染。
- 修改 `apps/mobile/app/_layout.tsx`：在 RootLayoutNav 顶层挂载 `<PrivacyAgreementDialog />`。
- tsc --noEmit 校验：新文件无类型错误（member-benefits/webdav-sources/SkeletonBlock/networkUtils 等处为存量报错，与本次无关）。
