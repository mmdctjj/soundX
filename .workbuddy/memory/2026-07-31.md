# 2026-07-31

## 首次启动隐私协议弹窗（feat/test 分支）

- 需求范围（经用户两次确认）：仅 **mobile 的 Android 平台** 实现，iOS / mini / harmony 均不做。
- 新增 `apps/mobile/src/components/PrivacyAgreementDialog.tsx`：
  - 标题"欢迎使用 声仓"，正文含《用户协议》《隐私政策》蓝色（#1677ff）可点链接，链接地址取自会员登录页 `app/member-login.tsx`：
    - 用户协议：https://www.audiodock.cn/docs/user-agreement/
    - 隐私政策：https://www.audiodock.cn/docs/privacy-policy/
  - 首次启动检测：AsyncStorage key `privacy_agreement_accepted`，未同意则弹窗；读取失败时兜底也弹窗。
  - "同意并继续"：写入标记并关闭弹窗；"不同意"：`BackHandler.exitApp()` 退出 App。
  - `onRequestClose` 拦截返回键，防止绕过选择；仅 `Platform.OS === "android"` 渲染。
- 修改 `apps/mobile/app/_layout.tsx`：在 RootLayoutNav 顶层挂载 `<PrivacyAgreementDialog />`。
- tsc --noEmit 校验：新文件无类型错误（member-benefits/webdav-sources/SkeletonBlock/networkUtils 等处为存量报错，与本次无关）。

## 2026-08-03 设置页"关于"模块新增用户协议和隐私政策入口

- 需求：在 mobile、mini、harmony 三个子项目的设置页"关于"模块下新增两个选项：用户协议、隐私政策，默认浏览器打开会员登录页对应的两个链接。
- 实际改动：
  - `apps/mobile/app/settings.tsx`：在"关于"section 的 experienceProgram 之后用 `renderActionRow` 插入两项，`Linking.openURL` 打开对应链接；接入埋点。
  - `apps/mini/src/pages/settings/index.tsx`：在"关于"section 末尾插入两项，**Taro 没有 `openUrl` API**，实际改为 `Taro.setClipboardData` 复制链接 + Toast 提示"链接已复制，请在浏览器中打开"（i18n 中已有 `settings.linkCopied` key）。如需小程序端 web-view 跳转，后续可单独做。
  - harmony 未修改：工作区里 `apps/harmony` 在 git 中追踪的文件数为 0，没有任何 ets 源代码，只有 build 缓存——鸿蒙仓库在外部，需用户手动补充。
- 复用 i18n key：`settings.userAgreement` / `settings.privacyPolicy`（zh-CN："《隐私政策》"/"《用户协议》"，en："Privacy Policy"/"User Agreement"），无需新增翻译。
- tsc 校验：mobile 无错误；mini 仅剩 3 处 pre-existing unused-variable 警告，与本次改动无关。
- 链接地址（与 `apps/mobile/app/member-login.tsx` 一致）：
  - 用户协议：https://www.audiodock.cn/docs/user-agreement/
  - 隐私政策：https://www.audiodock.cn/docs/privacy-policy/
