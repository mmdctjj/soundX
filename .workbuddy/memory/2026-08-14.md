# 2026-08-14 工作日志

- 为 AudioDock（声仓）制定社区向内容策略方案，交付 `docs/content-strategy.md`。
- 用户确认目标：社区与口碑/开源优先、图文为主、要完整方案+首月选题。
- 方案要点：品牌信息屋、4 类人群画像、6 大内容支柱、渠道矩阵（官网博客/公众号/GitHub/少数派等）、首月 30 天具体选题（含借 release:beta 钩子的 Beta 招募文）。

## 鸿蒙 PersonalPage TTS 入口修复

- **问题**：`feat/test` 分支的 `apps/harmony/products/entry/src/main/ets/pages/PersonalPage.ets` 把 `feat/hm` 上 `5fd00098 feat(harmony): 会员中心/微信支付/扫码登录/TTS 能力接入与入口收敛` 提交里对 PersonalPage 的改动整段漏了。表现：点「TTS 转换」菜单弹"该功能暂未在鸿蒙端开放"。
- **根因**：合并冲突时 `PersonalPage.ets` 的 TTS 入口 hunks 被当作 ours 丢掉，TTS 页面 + 网络层 API + i18n 都进了，但入口没接。
- **修复**：用 `git show 5fd00098 -- PersonalPage.ets > /tmp/personal.patch` 导出完整 patch，`git apply --3way` 触发 5 处冲突（import / @State / initData / handleMenuAction case / 函数定义区），按"保留 openStoreDebug + 加 tts/incremental/full/compact/扫码登录/任务中心"原则逐个 resolve。
- **最终**：19 个 import、5 个 handleMenuAction case、11 个新函数（`openTtsTasks`/`handleOpenScanEntry`/`handleOpenTaskCenter`/`jumpToAppMarketDebug`/`loadVipStatus`/`checkRunningImportTask`/`startTasksPoll`/`handleUpdateLibrary`/`startImportTask`/`ImportProgressOverlay` 等）齐备；括号配对 diff=0；1140 行 / 38323 字符。
- **未做**：未跑 hvigorw/Harmony 编译验证，需要用户侧在 DevEco Studio 里 Build 一下确认。
