pub fn register() -> Result<(), String> {
    // Asset protocol is configured in tauri.conf.json and serves media files
    // via the $DOWNLOAD scope. No additional registration needed.
    Ok(())
}
