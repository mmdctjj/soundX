# 2026-08-29

## 排查 hub.audiodock.cn 镜像拉取极慢（NAS 42 分钟 / 本机 24 分钟）

### 结论：服务端全局出口带宽被限制在 ~3.4 Mbps，是硬瓶颈

在 Mac 上做了三轮实测（非猜测）：

| 实验 | 结果 |
|---|---|
| 单连接 curl 拉最大层 | 430 KB/s |
| 经 Clash 代理 vs 直连 | 420 KB/s vs 407 KB/s — **无差异，代理不是瓶颈** |
| **4 连接并行** | 聚合 **409 KB/s**，每条仅分到 73~191 KB/s — **全局带宽上限，非每连接限速** |
| 完整 docker pull | 跨 3 轮累计约 19.5 分钟下完 625.7 MB |

**关键推论：提高 `max-concurrent-downloads` 无效**（并发只是平分同样的 430 KB/s）。

### 「下载到 80% 就卡住」的成因 —— 巨层长尾

arm64 镜像 33 层 / 596.7 MiB 压缩、2.52 GB 解包后，层大小极度不均：

- 第 1 层 **273.6 MiB = 总量 46%**
- 前 3 层（273.6 + 111.6 + 77.3）= **总量 77.5%**
- 其余 30 层合计仅 32.5 MiB（5.5%）

所以：小层秒下完 → 进度快速爬到 ~20% → 只剩 3 个巨层慢慢爬；到 **~80% 时并发从 3 塌缩到 1**（只剩那个 273.6 MiB 层在传），观感就是「卡住」。该层在 430 KB/s 下单算就需 **11 分钟**，这是硬下限。

### 服务端架构缺陷（响应头实证）

- 链路：`客户端 → Caddy(443) → Harbor nginx → ECS 系统盘`
- blob 返回 **HTTP/2 200 直出，无 307 跳转对象存储** → 625 MB 全走 ECS 公网带宽
- Harbor `external_url` 未正确配置：`WWW-Authenticate: realm="http://39.106.11.191:8080/service/token"`
  （明文 + 裸 IP + 8080，应为 `https://hub.audiodock.cn`）
- ECS `39.106.11.191`（阿里云北京），公网带宽约 3~4 Mbps

### 本机 Docker 代理配置（重要坑）

`~/.docker/daemon.json`：
```json
"proxies": { "default": {
  "httpProxy": "http://127.0.0.1:7890",   // Clash Verge
  "httpsProxy": "http://127.0.0.1:7890",
  "noProxy": "localhost,127.0.0.1,*.local"   // 未排除 hub.audiodock.cn
}}
```
- 吞吐上无影响（Clash 规则模式，国内直连）
- **但 Clash 一停，docker pull 6 分 32 秒零字节进度**（daemon 仍指向 7890 却无人监听）→ 单点故障
- 建议 `noProxy` 加上 `hub.audiodock.cn,.audiodock.cn,39.106.11.191`

### 已排除的因素

- DNS 24ms / TLS 握手 165ms — 控制面健康
- 磁盘：宿主机剩 50G，Docker.raw 逻辑 460G 但**实际仅占 1.4G**（稀疏文件）
- `.dockerignore` 已正确排除 `video/`(610M)、`node_modules`(2.8G)、`venv`

### 后续优化方向（按性价比）

1. **服务端接 OSS/CDN 做 307 跳转** — 唯一能突破带宽上限的办法
2. **拆分 273.6 MiB 巨层** — 缩短长尾，改善观感（但不提升总吞吐，因是全局限速）
3. **瘦身镜像** — 2.52 GB 解包体积偏大，可换 slim 基础镜像
4. 修 Harbor `external_url`、去掉一层反代（Caddy/nginx 二选一）

---

## 参考 AudioCast（`~/Documents/projects/AudioCast` 官方 sample）给 AudioDock AVSession 通知栏播控补三项细节

### 背景
AudioDock 的 `services/avSessionService.ets` 已实现通知栏/控制中心播控卡片完整链路，比官方 AudioCast 更精细（activate 推迟到首次播放、setBackgroundPlayMode、指纹去重、进度不上报防 isReportOverSize）。AudioCast 是干净标准教材，AudioDock 是真机踩坑迭代版。

### 三点对齐改动（2026-08-29）

| # | 改动 | 位置 | 价值 |
|---|------|------|------|
| 1 | `bindLaunchWantAgent` 加 `wantAgentFlags: [wantAgent.WantAgentFlags.UPDATE_PRESENT_FLAG]` | `services/avSessionService.ets:367` | 防止通知栏点回重复拉起 EntryAbility |
| 2 | `pushPlaybackState` + `doPushPlaybackState` 加 `playMode: PlayMode` 参数，透出 `loopMode` 字段 | `services/avSessionService.ets:212-301` | 通知栏/控制中心显示循环模式图标 |
| 3 | 跳过 bufferedTime — PlayerStore 没有 bufferedMs 字段 | — | — |

### `avSession.LoopMode` 枚举（系统只认这 4 种，自定义模式必须就近映射）

- `LOOP_MODE_SEQUENCE` = 0（顺序播放）
- `LOOP_MODE_SINGLE` = 1（单曲循环）
- `LOOP_MODE_LIST` = 2（列表循环）
- `LOOP_MODE_SHUFFLE` = 3（随机播放）
- `LOOP_MODE_CUSTOM` (11+) = 4

PlayerStore.PlayMode 一对一映射：SEQUENCE→0, LOOP_SINGLE→1, LOOP_LIST→2, SHUFFLE→3

### AudioCast 与 AudioDock 通知栏播控对比备忘

| 维度 | AudioCast | AudioDock |
|------|-----------|-----------|
| createAVSession tag | "PLAY_AUDIO" | "AudioDockPlayer" |
| activate 时机 | initAVSession 立即 | **首次真正播放**（防"空激活"） |
| setBackgroundPlayMode | 不调 | activate 后调 ENABLE_BACKGROUND_PLAY |
| 元数据下发 | 每 setAVMetadata 都下发 | **指纹去重**（id+title+artist+album+duration） |
| 封面 | 同步本地资源 | **异步 http → PixelMap → 补发** |
| 进度上报 | timeUpdate 实时 setAvSessionPosition | **仅 seek 跳变/状态翻转/倍速变化**（防 isReportOverSize 限频） |
| 监听事件 | play/pause/stop/seek | + playNext/playPrevious/setSpeed |
| 后台长时任务 | BackgroundMode.AUDIO_PLAYBACK 枚举 | ['audioPlayback'] 字符串数组（API 18+ 等价） |

### 想补还没补的（必要时再做）

- `setExtras({'requireAbilityList': ['url-cast']})` — 投屏能力声明，AudioDock 暂不需要
- `setAVMetadata.filter` (TYPE_DLNA/TYPE_CAST_PLUS_STREAM/TYPE_CAST_PLUS_AUDIO) — 同上
- `bufferedTime` — PlayerStore 缺 bufferedMs 字段；要么在 IAudioPlayer 暴露 bufferedTime，要么不报
- `setLoopMode` 系统命令处理 — 让用户能在控制中心切循环模式（当前只透出 loopMode，没处理入站事件）

## 封面/头像分级加载（四端 + 后端）

### 需求与规则（已确认）
- 后端 `GET /image/optimize?src=&w=&q=&fmt=` 已完备（`services/api`），本次只加了 preGenerate 档位 `[300,600,900,1200]`。
- **规则按渲染位置分档，不按图片类型**：
  - 档位 `[96,128,300,600,900,1200]`；`w` 量化向上取整；`q=72`、`fmt=webp`。
  - **≤300 恒压缩（不分内外网）** —— 列表场景瓶颈是解码内存（50 张 3000×3000 原图 = 1.8GB 位图必 OOM），与网速无关。
  - **>300（Hero/全屏封面/艺术家大头像）才分内外网**：内网原图直连，外网 optimize。
- 外部源（Subsonic/Emby）不动；`/music/` 后端 `resolveLocalSource` 返回 null → 必 404，四端统一只放行 `/covers/`。

### 各端落地（对齐同一套档位语义：`width` = 目标设备像素宽，非 CSS）
- **desktop**：新增 `utils/imageBucket.ts`；`trackResolver.resolveArtworkUri` 单点收敛（≤300 恒压缩 / >300 读 `isCurrentInternalAddress`，已导出）；`utils/index.ts getCoverUrl(path,id,width=300)` 透传。删了 `Player/index.tsx` 里未用的本地 getCoverUrl 死代码。`useMediaSession` 显式 `format:"jpeg"`（OS 播控不认 webp）。`downloadManager` 封面强制 300（会嵌入 ID3，避免 5MB 原图）。
- **mini**：新增 `utils/image.ts`（同步 `Taro.getStorageSync` + 模块级缓存，缓存同时记录 baseURL 变化自动重算，避免与 request.ts 循环依赖）；9 个页面删重复 getImageUrl 副本改为 2 行 wrapper；所有 `<Image>` 加 `webp` 属性（iOS<14 需要）。注意：mv 页的 `getImageUrl(mv.path)` 是**视频地址**误用，已改为 `${getBaseURL()}${mv.path}`。
- **harmony**：新增 `utils/networkMode.ets`（AppStorage `isInternalNetwork` 键）；`image.ets getImageUrl(path,width=300)`；`CoverImage.ets` 加 `@StorageLink + @Watch('onNetworkModeChange')` 重算 URL（imgWidth 是 vp，按 ×2 估算）；刷新钩子挂 EntryAbility.initialize / AuthStore.loadFromStorage / switchServer。`MVDetailPage.resolveVideoUrl` 的视频地址误用已改。构建 BUILD SUCCESSFUL；7 个 BuildProfile.ets 被翻 debug 需提交前还原。
- **mobile**：新增 `utils/imageBucket.ts` + `utils/networkMode.ts`（模块级 `_isInternal` 同步读，`refreshNetworkMode()` 异步刷新）；`image.ts getImageUrl(path,placeholder,width=300)` 保持同步；AuthContext 在 autoSwitchServer/loadAuthData/switchServer 三处挂 `refreshNetworkMode()`。`trackResolver.resolveArtworkUriForPlayer` 默认 300（会 cacheCover 落盘，避免缓存 5MB 原图）。`mv/[id].tsx` 视频地址误用已改。

### 关键坑（跨端一致）
- `getImageUrl` 必须保持同步纯函数（mobile 40+/harmony 30+ 调用点），网络判定走「内存变量 + 异步刷新」。
- mv 页 `getImageUrl(path)` 拼视频地址是历史误用，三端（mini/harmony/mobile）都顺手修了。
- 移动端既有 `setTracks(tracksRes.data)` 的 `ILoadMoreData<Track>` 类型错误（artist 页）与本次无关，未处理。

### 事故记录（重要）
调研阶段误用 `git stash` 做基线对比，`git stash push`（含未跟踪文件）失败但随后的 `git stash pop` 弹出了 **2026-05-20 的旧 stash**，导致 `apps/desktop/dist-electron/main.js`、`preload.mjs` 冲突（UU）。已 `git checkout --ours` + `git reset` 恢复到 HEAD（这两个是构建产物），备份在 `/tmp/audiodock_conflict_backup/`。教训：不要用 `git stash` 做基线对比，改用 `git worktree` 或直接看 diff。

---

## 补完"投播"链路核心两点（2026-08-29 18:00）

### 用户反馈：通知栏/控制中心不显示音频卡片

澄清术语：华为的"投播" = 系统通知栏/控制中心/锁屏上的媒体播控卡片（不是把内容推远端设备）。
前次做的三点对齐（UPDATE_PRESENT_FLAG + loopMode + bufferedTime 跳过）都是锦上添花，**真正的链路缺失**没补。

### 核心根因：缺 `setExtras({'requireAbilityList': ['url-cast']})`

不调这个 setExtras，系统不认 AVSession 是音频代理，**通知栏卡片不显示**。
对齐 AudioCast AVSessionController.ets:68。

### 改动

| # | 文件 / 位置 | 改动 |
|---|------------|------|
| 1 | `services/avSessionService.ets` `installAVSessionService` | 加 `await session.setExtras({'requireAbilityList': ['url-cast']})`，放在 createAVSession 之后、activate 之前 |
| 2 | `services/avSessionService.ets` 新增 `mapLoopModeToPlayMode` 反向映射 | 配套 #3 |
| 3 | `services/avSessionService.ets` `bindControlCommands` | 加 `session.on('setLoopMode', handler)` 监听；用户在控制中心切循环模式 → 反向映射 → 写回 PlayerStore.playMode |

### AudioCast vs AudioDock 通知栏播控链路对照（对齐后）

| 项目 | AudioCast | AudioDock |
|------|-----------|-----------|
| createAVSession | ✓ | ✓ |
| **setExtras url-cast** | ✓ AVSessionController.ets:68 | ✅ 2026-08-29 补 |
| setLaunchAbility + wantAgentFlags | ✓ | ✓（前次补） |
| activate 时机 | 立即 | 推迟到首次播放（更稳） |
| setBackgroundPlayMode | 不调 | ✓（API 24+，比 AudioCast 更全） |
| setAVMetadata filter | TYPE_DLNA/TYPE_CAST_PLUS_STREAM/TYPE_CAST_PLUS_AUDIO | ❌ 暂未加（不投远端不需要） |
| setAVMetadata mediaImage | 同步本地 | ✓ 异步下载解码 |
| setAVPlaybackState loopMode | ✓ | ✅ 前次补 |
| setAVPlaybackState bufferedTime | 14000ms 硬编码 | ❌ PlayerStore 无 bufferedMs |
| 监听 play/pause/stop/seek | ✓ | ✓ |
| 监听 playNext/playPrevious/setSpeed | 不监听 | ✓（比 AudioCast 监听更全） |
| **监听 setLoopMode** | ✓ ControlAreaComponent.ets:252 | ✅ 2026-08-29 补 |

### 编译验证

hvigor BUILD SUCCESSFUL in 6s242ms。

### 还差什么（这次没做）

- **`setAVMetadata.filter` 字段**：声明支持哪些投播协议。AudioDock 不投远端设备所以不需要，但加上对正确性更稳。
- **`bufferedTime`**：通知栏滑块拖动时显示真实可播放时长。需要 PlayerStore 加 `bufferedMs` 字段 + AVPlayerBackend 暴露 bufferedTime 事件。这是 PlayerStore + IAudioPlayer 联合改动，留作独立任务。

## 补：mini 漏改文件修正

初次只改了 9 个详情页，**漏了**另外 6 个文件各自复制的本地 `getImageUrl`（仍拼原图）：
`pages/library/index.tsx`、`pages/index/index.tsx`（首页/曲库主页，最常看）、`components/MiniPlayer`、`components/StackedCover`、`components/PlaylistModal`、`context/PlayerContext`（`bgAudioManager.coverImgUrl` 通知栏封面，用 128 档）。已全部改为 import 共享 `utils/image` 的 `getImageUrl`，删除了未用的 `getBaseURL` 导入，并补 `webp` 属性。教训：改 mini 这种「每文件复制一份工具函数」的代码，要用 `grep -rn "const getImageUrl"` 全量扫，不能只靠详情页清单。

---

## mobile 真机日志排查 + 3 个组件漏接 optimize 修复（2026-08-29 15:00）

### 排查过程

1. 用户怀疑「mobile 声仓页面所有列表封面没走 optimize」，先让 adb 连真机验证
2. `adb devices` 一开始 `unauthorized` → `adb kill-server && adb start-server` 后变 `device`（88b7b21）
3. `adb reverse tcp:8081 tcp:8081` + 后台起 metro (`npx expo start --port 8081`)
4. **关键发现**：App 是已装的 release 版（非 expo-dev-client），但 metro 仍能 8081 服务（服务下载口）——可通过 `adb shell am start -n com.audiodock.app/.MainActivity` 拉起
5. 清 logcat 抓 `ProgressiveDecoder` / `unknown image format`，立刻命中 2 条：

```
W ProgressiveDecoder: unknown image format,
{uri: http://192.168.1.6:8860/covers/周杰伦-三年二班.mp3.jpeg,
 firstEncodedBytes: 7B22636F6465223A3430,  ← hex 解码 = '{"code":40'
 length: 129}
```

6. hex 解码证明 body 是 JSON 错误响应而不是图片：服务器 `/covers/...` 返回 `{"code":404,...}` JSON，前端当 JPEG 解码失败

### 真正漏接的 3 个组件（grep 反查 `${getBaseURL()}${...cover...}`）

| 文件 | 行号 | 场景 |
|---|---|---|
| `src/components/MiniPlayer.tsx` | 56 | 迷你播放条封面（每页面底部常驻） |
| `src/components/PlaylistModal.tsx` | 182 | 播放列表弹窗行内封面 |
| `src/components/PlaybackNotification.tsx` | 58 | 通知中心封面（15s 自动消失的那种弹窗） |

3 处都是同一个反模式：手写 `${getBaseURL()}${x.cover}` 且**完全没 import `getImageUrl`**。

### 修复

| 文件 | 改动 |
|---|---|
| MiniPlayer.tsx | 删 `getBaseURL` import；`getImageUrl(currentTrack.cover, "https://picsum.photos/100", 96)` |
| PlaylistModal.tsx | 删 `getBaseURL` import；`getImageUrl(item.cover, "https://picsum.photos/100", 96)` |
| PlaybackNotification.tsx | 删 `getBaseURL` import；`getImageUrl(notification.track.cover, undefined, 128)` |

档位选择依据：MiniPlayer/PlaylistModal 都是小列表缩略图（≈48dp × 2 ≈ 96 设备像素），通知中心封面稍大（≈64dp × 2 = 128）。都 ≤300，恒压缩。

`tsc --noEmit` 验证：3 个文件**无类型错误**（其它 5 个文件历史存量错误与本次无关）。

### 验证

`pm clear com.audiodock.app` 清空缓存（包括之前 404 落盘的脏 JPEG 缓存），重启 App，**15 秒内 0 条 ProgressiveDecoder 报错**。

### 关键诊断技巧（复用价值高）

- **hex 解码识 JSON 兜底**：`firstEncodedBytes: 7B22636F6465...` 前 8 字节是 `{"code"` 的 ASCII，立刻识别为「服务端把 404 JSON 当图片返回了」
- **URL 路径反推前端**：日志里 URI 是 `/covers/...` 直连 → 一定不是走 `getImageUrl`（那个会拼 `/image/optimize?src=...`）
- **`adb pm clear`** 清脏缓存必备，否则旧 404 缓存一直命中显示 broken

### 仍未修的问题（独立问题）

- 服务器上 `/covers/周杰伦-三年二班.mp3.jpeg` 文件本身不存在 → 即使走 optimize 也 404。需要在曲库扫描时给专辑补默认封面，或修源头文件。这跟 mobile 端代码无关。
