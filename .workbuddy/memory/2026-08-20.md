# 2026-08-20 工作日志

## hm 子项目：隐藏会员支付入口 + 个人页皇冠逻辑调整

需求：隐藏所有「点击去开通」入口；个人页皇冠仅会员可见（金色）、点击进会员权益页，非会员不展示。

### 改动文件汇总（统一标记 `// [软下线]`，全局搜索可一键恢复）

| 文件 | 改动 |
|---|---|
| `PersonalPage.ets` | 皇冠 `if (isPlusVip)` 条件渲染（金色 #FFD700），`onCrownClick` 简化为仅会员跳 MemberBenefitsPage；TTS 入口弹窗、扫码登录弹窗去掉【去开通】按钮与跳转 |
| `SettingsPage.ets` | 车机模式 VIP 弹窗去掉【前往会员页】按钮与跳转 |
| `TtsTaskListPage.ets` | 非会员弹窗去掉【去开通】按钮与跳转 |
| `TtsCreateTaskPage.ets` | 同上 |
| `MemberDetailPage.ets` | 非会员视角「会员权益」按钮加 `if (false)` 软下线 |
| `PlayerPage.ets` | 新增 `isPlusVip` 状态 + `loadVipStatus()`；同步控制按钮 `if (isPlusVip)` 条件渲染，非会员隐藏 |
| `MemberBenefitsPage.ets` | 不动（支付按钮已是 `if (false)` 隐藏状态） |

### 补充改动（用户反馈后追加）
- `PlayerPage.ets`：非会员隐藏同步控制（person 图标）入口
- `PersonalPage.ets`：菜单中 TTS 有声书入口增加 `isPlusVip` 判断，非会员不展示

### 遗留问题
- 本机 OpenHarmony SDK 为 6.0.0(API20)，项目要求 6.0.2(API22)，hvigor 命令行构建无法跑通，仅做了人工代码复查。真机/模拟器验证待后续。

### 追加：皇冠三段逻辑细化（用户反馈）

PersonalPage.ets 新增 `isPlusLoggedIn` 状态（`plus_token` 是否存在），皇冠显隐改为三段：
- 未登录会员体系 → 灰色皇冠，点击跳 `MemberLoginPage`
- 已登录会员 → 金色皇冠（#FFD700），点击跳 `MemberBenefitsPage`
- 已登录非会员 → 隐藏皇冠

改动点：`loadVipStatus()` 中写入 `isPlusLoggedIn`；皇冠渲染条件 `if (isPlusVip || !isPlusLoggedIn)`；`onCrownClick` 加回未登录跳 MemberLoginPage 分支。

### 追加：会员皇冠跳会员详情页（用户反馈）
`onCrownClick` 中已登录会员从跳 `MemberBenefitsPage` 改为跳 `MemberDetailPage`。

### 追加：MV 列表倒序（用户需求）
`LibraryPage.ets` L254：音乐库 MV tab 列表 `this.mvs = res.list.slice().reverse()`，最新添加排前面。仅一处赋值，无分页加载。

### 追加：数据源管理页举报按钮优化（用户需求）
`SourceManagePage.ets`：举报按钮从感叹号 SymbolGlyph 图标改为 Text 文字「举报数据源」（宽度自适应）；提交举报按钮加 `reportSubmitting` 状态 + 1s setTimeout 模拟 loading，再弹成功提示。
