# 2026-08-16 工作日志

## 车机模式「前往会员页面」点击无响应（hm 端修复）

- **背景**：昨天实现的车机模式 SettingsPage VIP 门控，用户实测点击「前往会员页面」按钮没反应。
- **根因**：ArkUI ETS `promptAction.showDialog({ buttons: [{ text, color }] })` 的 button **不支持 `onClick` 字段**，必须 `await` 返回值拿到 `ShowDialogSuccessResponse.index` 判断点击的是哪个按钮（0=取消 / 1=前往会员）。原代码用 `.catch(() => undefined)` 直接 fire-and-forget，点了之后什么都不做。
- **修改**（`apps/harmony/products/entry/src/main/ets/pages/SettingsPage.ets` `handleCarModeChange`）：
  ```ts
  const result = await promptAction.showDialog({
    title: t('settings.vipOnly'),
    message: t('settings.carModeVipOnly'),
    buttons: [
      { text: t('common.cancel'), color: '#888' },
      { text: t('settings.goToMemberPage'), color: '#3F51B5' },
    ],
  }).catch((): undefined => undefined);
  if (result?.index === 1) {
    router.pushUrl({ url: 'pages/MemberBenefitsPage' }).catch(...);
  }
  ```
- **教训 / 通用规则**：ArkUI ETS 所有「点了做某事」的多按钮对话框，必须 `await` + 读 `result.index`，不能用 button.onClick。同一坑 PlayerPage 的「立即开通」按钮也存在（`PlayerPage.ets` 有注释「按 mobile 行为简单提示，跳转交由用户主动去 PersonalPage」），用户没要求改就暂不动。
- **已验证**：`hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL。

## 车机模式昨日实现收尾（hm 端，从 8-15 接续）

- 昨天已完成的 6 个文件改动全部保留：`EntryAbility.ets` / `SettingsPage.ets` / `MainPage.ets` / `GlobalBottomBar.ets` / `SquirrelAgent.ets` + 新增 `EmbeddedPlayerPanel.ets`。
- AppStorage 跨页面同步方案 (`@StorageLink('carModeEnabled' / 'carPanelsSwapped' / 'screenBottomInset')`) 验证通过。
- 编译链路确认（关键环境变量）：`DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk` + PATH 含 hvigorw/bin；shell 里残留的 `~/Library/OpenHarmony/Sdk`（无 API 22）会干扰，必须显式覆盖。
- 待真机验证（无模拟器）：① 横屏锁定首启是否弹用户确认框；② 9:16 面板在不同分辨率机型下宽度是否合适；③ swap 按钮位置；④ SquirrelAgent 贴左后 bubble 方向（`onSideRight()` 判断基于 posX，可能溢出到右半区）。

## 自动横竖屏开关无效（hm 端修复）

- **现象**：SettingsPage「自动横竖屏」开关开启后，应用不会跟随系统旋转；关闭状态下只要横屏也会被强制竖屏。
- **根因**（双重）：
  1. `SettingsPage.handleAutoOrientationChange` 只写 `kvStore`，**从未调用过 `setPreferredOrientation`**，开关在 UI 层无效。
  2. `MainPage.applyOrientation(carMode)` 关闭车机模式时强制 `PORTRAIT`，**完全没用 autoOrientation 字段**——优先级链设计缺一环。
- **修复**（3 文件）：
  1. `EntryAbility.ets`：`initialize()` 末尾从 kvStore 读 `autoOrientation` 写入 AppStorage，让 MainPage 能 `@StorageLink` 订阅。
  2. `SettingsPage.ets`：
     - `@State autoOrientation` → `@StorageLink('autoOrientation') autoOrientation`
     - `loadSettings` 删除 `this.autoOrientation = await this.readBool(...)`（AppStorage 已提供）
     - `handleAutoOrientationChange(val)`：写 kvStore + 写 AppStorage + 立即调 `applyOrientationImmediate()`（不等 MainPage 重建）
     - 新增 `applyOrientationImmediate()` 复用优先级链（carMode > autoOrientation > PORTRAIT）
     - 新增 `import { window } from '@kit.ArkUI'`
  3. `MainPage.ets`：
     - 新增 `@StorageLink('autoOrientation') @Watch('onAutoOrientationChange') autoOrientation`
     - 新增 `onAutoOrientationChange()` → `applyOrientation()`
     - `applyOrientation(carMode)` 重构为无参 `applyOrientation()`，内部读两个 `@StorageLink` 字段算优先级
     - `aboutToAppear` 去掉 `if (this.carModeEnabled)` 判断，总是 `applyOrientation()`（启动态就能跟随 autoOrientation）
- **优先级链（统一在 MainPage.applyOrientation / SettingsPage.applyOrientationImmediate）**：
  ```
  carModeEnabled=true → LANDSCAPE（车机模式锁横屏，最高优先级）
  否则 autoOrientation=true → AUTO_ROTATION（跟随系统）
  否则 → PORTRAIT（锁定竖屏，默认）
  ```
- **新发现**：HarmonyOS `window.Orientation.AUTO_ROTATION` 枚举值在 SDK 22 (API 9) 编译通过，可用。
- **教训**：所有跨页面方向/布局开关，统一用 `@StorageLink` + `@Watch` + 优先级链，写在一个独立 `applyXxx()` 函数里；页面 handler 也调同一份函数，避免栈顶页面差异。
- **已验证**：`hvigor BUILD SUCCESSFUL`。

## 自动主题（跟随系统主题）开关无效（hm 端修复，第二轮）

- **第一版无效的原因**：用了 ArkUI 的 `mediaquery.matchMediaSync('(prefers-color-scheme: dark)')` 监听系统色变化 — **这是 Web 标准 API，HarmonyOS 上不会真正响应系统设置里的深浅色切换**。编译能过、`MediaQueryListener` 实例能创建、`matches` 属性也能读到值，但 `change` 事件在真实设备上根本不触发（Web 习惯容易踩的坑）。
- **正确方案**：用 HarmonyOS 自己的 `ConfigurationConstant.ColorMode.COLOR_MODE_DARK` + `UIAbility.onConfigurationUpdate(newConfig)` 回调。这是 HarmonyOS 系统级标准回调，**100% 可信**。
- **修改**（2 文件 + 1 新文件）：
  1. `context/ThemeAutoStore.ets`（重写）：
     - `import { ConfigurationConstant, common } from '@kit.AbilityKit'`
     - 删除 mediaquery 监听
     - `init(ctx: common.UIAbilityContext)`：EntryAbility 启动时注入 context
     - `setEnabled(enabled)` / `restore()`：开启时立即读 `ctx.config.colorMode` 同步到 themeMode
     - `onSystemColorChanged(colorMode: ColorMode | undefined)`：EntryAbility.onConfigurationUpdate 回调里调用，内部按 autoTheme 状态决定是否同步
     - `stop()`：EntryAbility.onDestroy 时清空 ctx
  2. `entryability/EntryAbility.ets`：
     - import 加 `ConfigurationConstant, Configuration` from '@kit.AbilityKit'
     - `onDestroy()`：新增 `themeAutoStore.stop()` 调用
     - **新增 `onConfigurationUpdate(newConfig: Configuration)` 回调**：调 `themeAutoStore.onSystemColorChanged(newConfig.colorMode)`
     - `initialize()` 末尾：替换 `themeAutoStore.restore()` → `themeAutoStore.init(ctx)` + `themeAutoStore.restore()`
  3. `pages/SettingsPage.ets`（仅注释微调）：
     - `handleAutoThemeChange` 调用方式不变（`themeAutoStore.setEnabled(val)`），注释由「mediaquery 监听」改为「onConfigurationUpdate 自动跳过覆盖」
- **类型坑**：`Configuration.colorMode` SDK 定义是 `ColorMode | undefined`（optional），必须 `?? LIGHT` 兜底，否则 ArkTS 严格模式编译失败（`Argument of type 'ColorMode | undefined' is not assignable to parameter of type 'ColorMode'`）。
- **教训 / 通用规则**：
  - **HarmonyOS 系统配置变更**（深浅色、字体大小、语言、横竖屏、density）一律走 `UIAbility.onConfigurationUpdate(newConfig: Configuration)`，**不要用 Web mediaquery**——后者只对 CSS 媒体查询生效，不会响应系统设置变更。
  - `@kit.AbilityKit` 的 `Configuration` / `ConfigurationConstant` / `ColorMode` 是标准答案。
  - 跨页面监听仍要放单例 store（仿 SyncStore），由 EntryAbility 注入 context；store 的 API 只接收外部通知 + 内部读 ctx，调用方不感知 isDark 细节。
  - Configuration.colorMode 是 optional（`ColorMode | undefined`），所有用到的地方必须兜底。
- **已验证**：`hvigor BUILD SUCCESSFUL`。
- **教训 / 通用规则**：
  - **HarmonyOS 系统配置变更**（深浅色、字体大小、语言、横竖屏、density）一律走 `UIAbility.onConfigurationUpdate(newConfig: Configuration)`，**不要用 Web mediaquery**——后者只对 CSS 媒体查询生效，不会响应系统设置变更。
  - `@kit.AbilityKit` 的 `Configuration` / `ConfigurationConstant` / `ColorMode` 是标准答案。
  - 跨页面监听仍要放单例 store（仿 SyncStore），由 EntryAbility 注入 context；store 的 API 只接收外部通知 + 内部读 ctx，调用方不感知 isDark 细节。
  - Configuration.colorMode 是 optional（`ColorMode | undefined`），所有用到的地方必须兜底。
- **已验证**：`hvigor BUILD SUCCESSFUL`。

## 设置页开关多次失效的根因模式总结（hm 端）

| 现象 | 真因 | 正确做法 |
|---|---|---|
| 自动横竖屏开关无效 | handler 只写 kvStore，MainPage 关闭车机模式时硬写 PORTRAIT，没用 autoOrientation | 优先级链 carMode > autoOrientation > PORTRAIT；AppStorage 跨页面 + MainPage/SettingsPage 各调一份 applyOrientation |
| 自动主题开关无效（第一次） | handler 只写 kvStore，没有任何代码读/监听系统色 | （错误尝试 mediaquery）→ 改用 ConfigurationConstant + onConfigurationUpdate |
| 「前往会员页面」点击无反应 | ArkUI showDialog button 不支持 onClick | 必须 `await` + 读 `result.index` |
| 设置页开关点不动（多轮） | Toggle 在 @Builder / @Prop 场景下重建触发鬼影 onChange | 最终方案：@StorageLink → 独立 @Component + @Link value + 透明热区 |
| **共性**：所有失效场景的根因都是 **handler 写了状态但没真正驱动 UI/系统响应**。写之前先想：① 谁读这个状态？② 谁触发副作用？③ 副作用链路在哪？

## 我的页面右上角三个图标按钮对齐 mobile（hm 端）

- **背景**：用户反馈 hm PersonalPage header 右上角三个图标和 mobile 不一致 —— 资源管理是 `folder`（文件夹），设置是 `list`（列表）；mobile 是 `server-outline`（服务器）和 `settings-outline`（齿轮）。
- **差异点**（mobile `personal.tsx` 986~1009 行 → 这里直接列对齐表）：
  | 功能 | mobile (Ionicons) | hm 改前 | hm 改后 (AppIcon → sys.symbol) |
  |---|---|---|---|
  | 扫码登录 | `AntDesign.scan` | `scan` (qrcode) | 不变，对齐 |
  | 任务中心 | `Ionicons.list-outline` | `list-outline` (list_bullet) | 不变，对齐 |
  | 资源管理 | `Ionicons.server-outline` | `folder` (folder) ❌ | `server-outline` (archivebox) |
  | 设置 | `Ionicons.settings-outline` | `list` (list_bullet) ❌ | `settings-outline` (gearshape) |
- **修改**：
  1. `apps/harmony/products/entry/src/main/ets/components/AppIcon.ets`：
     - `AppIconName` 加 `server-outline` `settings-outline` 两个成员
     - `ICON_MAP` 注册：`server-outline → sys.symbol.archivebox`（**降级**）；`settings-outline → sys.symbol.gearshape`（与 gear 同源）
  2. `apps/harmony/products/entry/src/main/ets/pages/PersonalPage.ets` HeaderBuilder 第 731 / 737 行图标名替换。
- **HarmonyOS SDK 19 (API 19) sys.symbol 不可用清单**（实测 10903329）：
  - `server` ❌、`external_drive` ❌、`network` ❌
  - 最终可用：`archivebox` ✅（视觉接近"资源/服务器"）
- **教训 / 通用规则**：
  - **新 symbol 名先小代价试错**：直接编译看 10903329，确认可用再写正式代码。任意 3 个候选项（`server`/`external_drive`/`network` → `archivebox`）都是这个套路。
  - **同语义不同入口**用两个 AppIconName 各占一名（`gear` vs `settings-outline`），映射可同源；未来 gearshape 升级为填充态时两边一起改。
  - **mobile → hm 图标对齐**：先 grep mobile 的 Ionicons name 和 hm 的 AppIcon name 找差异，再按 `IconName → sys.symbol.X` 链路查 SDK 支持，最后编译兜底。
- **已验证**：`hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL。

## 扫码图标换成扫描框（hm 端二次修复）

- **背景**：上一轮改成 `sys.symbol.qrcode` 后用户反馈"这是二维码图标，不是扫码图标"——`qrcode` 是方块阵列，扫码是取景器框。
- **mobile 用的是 `AntDesign scan`**（取景器框），不是 `qrcode`。
- **候选验证**（依次 10903329 排查）：
  - `viewfinder` ❌
  - `qrcode_viewfinder` ❌
  - `barcode_viewfinder` ✅ ← 用这个
- **修改**：`AppIcon.ets` 的 `'scan'` 映射从 `sys.symbol.qrcode` → `sys.symbol.barcode_viewfinder`。PersonalPage 的调用无需改（name 仍用 `'scan'`）。
- **教训**：扫码/扫描图标按 SF Symbols 标准应选 `xxx_viewfinder` 后缀，而非 `qrcode`（这是二维码方块阵列）；HarmonyOS SDK 19 只支持 `barcode_viewfinder`（条形码取景器），没有 `qrcode_viewfinder`，也不算 `viewfinder` 通用名。

## 扫码登录改造为相机扫码（hm 端，对齐 mobile）

- **背景**：原 ScanPage 是手动输入 session id（mobile 是相机扫桌面端二维码）。用户明确选择"改为相机扫码对齐 mobile"，不走"加手动输入作为兜底"的妥协方案。
- **mobile 参考流**：`apps/xiaolu/.../app/scan.tsx`（`expo-camera` CameraView + 解析）→ claim → `app/scan-confirm.tsx`。hm 端要把 `expo-camera` 换成 HarmonyOS `ScanKit`。
- **技术栈对比**：
  | 维度 | mobile (Expo) | hm (HarmonyOS) |
  |---|---|---|
  | 扫码头 | `expo-camera` `CameraView` + 手写 QR 检测 | `@kit.ScanKit` `scanBarcode.startScanForResult`（官方 Default View） |
  | 权限 | `useCameraPermissions()` (expo) | `ohos.permission.CAMERA` + `abilityAccessCtrl.checkAccessTokenSync` |
  | 取消回调 | 手动监听 / close | 错误码 201 = 用户取消，silent |
- **关键 API**（API 10+）：
  - `canIUse('SystemCapability.Multimedia.Scan.ScanBarcode')` 设备能力检查。
  - `scanBarcode.startScanForResult(context, options): Promise<ScanResult>`：options = `{ scanTypes: [scanCore.ScanType.QR_CODE], enableMultiMode: false, enableAlbum: true }`。
  - 成功：`result.originalValue`；用户取消：rejected err.code 201。
- **权限检查的 3 个坑**（全部踩过）：
  1. `checkAccessTokenSync` 是 **2 参数**（不是 3 参数）：`(tokenId: number, permissionName: string)`，没有第三个 GrantStatus 入参。
  2. `tokenId` **不能从 context 拿**，要从 `bundleManager.getBundleInfoForSelfSync(bundleManager.BundleFlag.GET_BUNDLE_INFO_WITH_APPLICATION).appInfo.accessTokenId` 取。`bundleManager.ApplicationFlag` / `getApplicationInfoSync` 都不存在。
  3. `import { abilityAccessCtrl, common } from '@kit.AbilityKit'`；common.UIAbilityContext 用于 startScanForResult。
- **module.json5 权限声明**：
  ```json
  { "name": "ohos.permission.CAMERA", "reason": "$string:camera_permission_reason",
    "usedScene": { "abilities": ["EntryAbility"], "when": "inuse" } }
  ```
  + `base/element/string.json` 加 `camera_permission_reason` 字符串。
- **页面状态机**（`ScanPage.ets`）：
  - `unsupportedDevice`（canIUse 失败）→ 不可用文案
  - `checkingPermission`（首次进入）→ 加载态
  - `permissionGranted`（false）→ PermissionGate + "去扫码"按钮（点击拉起系统授权）
  - `permissionGranted`（true）→ 直接 `startScan()`（不弹中间页）
  - 拿到 QR → `parseScanLoginQR` + `scanLoginApi.claim` → `router.replaceUrl({ url: 'pages/ScanConfirmPage', params: { sessionId, secret } })`
- **i18n 新增键**（`scanLogin.*`）：`permissionDenied` / `unsupportedDevice`。`permissionDesc` / `grantPermission` / `aimHint` 沿用上一版命名空间。
- **构建踩坑**：WorkBuddy 沙箱隔离 + 旧构建产物 → `FSMoveObjectToTrashSync failed status -43`。解法：`hvigorw clean --no-daemon` 后重新 `assembleHap`。
- **已验证**：`hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL in 9s 836ms（clean 后）。
- **教训 / 通用规则**：
  - **HarmonyOS 权限检查标配**：`bundleManager.getBundleInfoForSelfSync(...)` 取 tokenId + `abilityAccessCtrl.createAtManager().checkAccessTokenSync(tokenId, permission)`。不要尝试 `getApplicationInfoSync` 等不存在的 API。
  - **官方 Kit 优先**：能调 ScanKit 就别自己撸相机预览；startScanForResult 自带 UI、权限、取消回调，省 80% 代码。
  - **构建缓存冲突**：WorkBuddy 沙箱里如果旧产物路径变了（safe-delete 失败），先 `hvigorw clean`，别死磕 assembleHap。
  - **mobile → hm 对齐**：先看 mobile 的"扫码页 + 确认页"是不是分两页的；是的话 hm 也要分两页（ScanPage → ScanConfirmPage），不要试图塞进一个页面。

## 详情页公共 Tab + MiniPlayer 改成跨页面常驻（hm 端，对齐 mobile）

- **背景**：用户要求把 ArtistDetailPage / AlbumDetailPage / PlaylistDetailPage / CollectionDetailPage / SearchPage 这 5 个详情页（外加 MainPage 自己的 Tab）底部公共 Tab + MiniPlayer 改成"跨页面常驻"，对齐 mobile 的 `app/_layout.tsx` 实现。
- **mobile 实现**：`app/_layout.tsx` 的 `<Stack>` 平级渲染 `<GlobalBottomBar />`，详情页**完全不引用** MiniPlayer，通过 `showBottomBar = !hideBottomBar` 控制显隐。`hideBottomBar` 名单不含 `album / artist / playlist / collection / folder`。
- **hm 端现状**：每个详情页**手动**在根 `Column` 末尾写 `GlobalBottomBar()`（5 处：`ArtistDetailPage:145` / `AlbumDetailPage:159` / `PlaylistDetailPage:100` / `CollectionDetailPage:79` / `SearchPage:206`）。Page 切换是真实页面跳转（`aboutToDisappear` → `aboutToAppear`），切换时 GlobalBottomBar 实例会反复销毁重建，虽然 `playerStore` 单例不丢，但 `@StorageLink('carModeEnabled')` 等订阅每次重建，新增详情页容易漏挂。
- **方案 A（采用）**：改用 `<Navigation>` + `<NavDestination>` 架构，把 5 个详情页 + MainPage 全部装进 Navigation 容器，GlobalBottomBar 在 Navigation 容器外平级渲染。
- **架构改动**：
  ```
  Index（启动器，保留 @Entry）
    └─ router.replaceUrl → RootShellPage（新增唯一 @Entry）
         └─ Stack {
              Navigation(pathStack) {
                ├─ MainTabs         (MainPageContent, 监听 mainTabIndex 切 3 个 Tab)
                ├─ ArtistDetail
                ├─ AlbumDetail
                ├─ PlaylistDetail
                ├─ CollectionDetail
                └─ Search
              }
              GlobalBottomBar()   ← 与 Navigation 平级,真正常驻
            }
  ```
- **改动文件（10 个）**：
  1. `pages/RootShellPage.ets`（新建，唯一 @Entry，路径栈 + destination builder + onPageShow 桥接 pendingNavDest）
  2. `components/GlobalBottomBar.ets`（加 `pathStack: NavPathStack` 入参，switchToTab 改 `pathStack.popTo({ name: 'MainTabs', animated: false })`）
  3. `pages/MainPage.ets`（`MainPage` → `MainPageContent` 去掉 @Entry + 拆掉 `Tabs({ barPosition: BarPosition.End })` 容器，改为 `currentIndex` 监听渲染 HomeTab/LibraryTab/PersonalTab，4 处 router.pushUrl 改 pathStack.pushPath。HomeTab 加 pathStack 入参）
  4. `pages/LibraryPage.ets`（LibraryTab 加 pathStack，Search/Artist/Album/Collection 跳改 pathStack，MV/Player 保留 router.pushUrl）
  5. `pages/PersonalPage.ets`（PersonalTab 加 pathStack，PlaylistDetail/AlbumDetail 跳改 pathStack）
  6. `pages/ArtistDetailPage.ets`（重写：`ArtistDetailPage` → `ArtistDetailContent`，去 @Entry，去 GlobalBottomBar import + 调用，加 pathStack + id 入参，AlbumDetail/CollectionDetail 跳改 pathStack，MVDetail/PlayerPage 保留 router.pushUrl）
  7. `pages/AlbumDetailPage.ets`（同 6 模式）
  8. `pages/PlaylistDetailPage.ets`（同 6 模式）
  9. `pages/CollectionDetailPage.ets`（重写：去 router import，跳 AlbumDetail 改 pathStack）
  10. `pages/SearchPage.ets`（`SearchPage` → `SearchContent`，去 GlobalBottomBar，AlbumDetail/ArtistDetail 跳改 pathStack）
  + `pages/PlayerPage.ets`（navigateToArtist/navigateToAlbum 改 `pendingNavDest` 桥接：`AppStorage.setOrCreate('pendingNavDest', { name, id })` + `router.back()`，由 RootShellPage.onPageShow 读取并 pushPath）
  + `pages/Index.ets`（`navigateByAuth` 中 `target = 'pages/RootShellPage'`）
  + `main_pages.json`（注册 `pages/RootShellPage`）
- **关键决策点**：
  - **PlayerPage 不迁入 NavDestination**：保留 @Entry + slide_from_bottom 全屏体验；用 pendingNavDest + router.back() 桥接跳 NavDestination 内页面。
  - **MVDetailPage 不迁入 NavDestination**：mobile 的 hideBottomBar 含 `mv`，全屏体验更纯粹。
  - **HomeTab/LibraryTab/PersonalTab 接收 pathStack**：通过 `@Prop` 直接传引用类型（ArkUI 引用传递），pushPath 操作全局栈。
  - **NavPathStack 类型不存 AppStorage**：ArkTS 不支持复杂类型存 AppStorage，所以独立 Page 跳 NavDestination 必须用 pendingNavDest 桥接。
- **静态检查通过**（环境无 hvigorw）：
  - 0 处 `router.pushUrl` 跳 NavDestination 内页面残留
  - 8 处 `router.pushUrl('pages/PlayerPage')` 保留（PlayerPage 独立 Page）
  - 6 个 Content 组件都 `export struct`
  - RootShellPage import 路径全对得上
  - 无 `any` / `catch (error: any)` 严格模式违规
- **未验证**：WorkBuddy 沙箱无 DevEco Studio / hvigorw，未跑过 `assembleHap`。需要 DevEco 真机/模拟器验证：
  1. 启动路径 Index → RootShellPage → 主页 OK
  2. 详情跳转 Home 专辑卡片 → AlbumDetail OK；返回 OK；进 PlayerPage OK
  3. Tab 切换：详情页点底部"声仓" → 跳回 MainTabs 切到声仓 OK
  4. PlayerPage 跳 ArtistDetail：先回主页 → 自动 pushPath 到详情 OK（pendingNavDest 桥接）
  5. 车机模式：左右分栏布局 + GlobalBottomBar 隐藏 MiniPlayer 行为不变
- **教训 / 通用规则**：
  - **鸿蒙端做"全局常驻底部栏"**：必须用 `<Navigation>` + `<NavDestination>` 架构（ArkUI 路由栈独立 Page 互相切换是真实页面跳转，没法像 RN Stack 那样父 Layout 永不卸载）。
  - **NavDestination builder 的入参**：用 `@Prop` 接收标量（`id: string`），引用类型用 `@Prop pathStack: NavPathStack = new NavPathStack()` 默认值 + 父组件传入真实实例。
  - **跨栈跳转（独立 Page → NavDestination 内页面）**：用 `pendingNavDest` AppStorage 标志位 + RootShellPage.onPageShow 桥接；不要试图把独立 Page 也迁入 NavDestination（除非确认 NavDestination 模式支持 slide_from_bottom）。
  - **NavDestination builder 内传 pathStack**：`{ pathStack: this.pathStack }`（同一个 NavPathStack 实例），不要 `new NavPathStack()` 会创建新栈。
  - **`router.back()` → `pathStack.pop()`**：NavDestination 内的页面返回必须用 NavPathStack，路由栈独立。`router.back()` 会把整个 Navigation 容器也弹掉。
- **待用户确认**：三个详情页里**没有 public/shared Tab**。最接近的是 ArtistDetailPage 的"合作专辑"横向区（非 Tab 切换），以及 AlbumDetailPage 的"单曲/MV"二选一 Tab。本次未涉及，待用户澄清是否要新增。

## 编译错误修复（49 个 → 0）

用户跑 `hvigorw assembleHap` 报 49 个 ArkTS 错误。逐类修完：

### 类别 1：自定义组件 `id` prop 与 ArkUI 保留属性冲突（4 个）
ArkUI `CustomComponent` 把 `id` 保留为通用属性（`CommonAttribute.id()`），自定义组件不能用 `id: string` 作 prop。
- **修复**：4 个 Content 组件（ArtistDetail / AlbumDetail / PlaylistDetail / CollectionDetail）的 `id: string = ''` 改为 `entityId: string = ''`，内部所有 `this.id` 引用同步改为 `this.entityId`，RootShellPage 注入处也改。
- **教训**：ArkUI 自定义组件 prop 名要避开 ArkUI 保留属性（id / width / height / onClick 等）。**始终用业务语义化名字**（entityId / artistId / albumId），不要用裸 id。

### 类别 2：内联对象字面量类型禁止（18 处）
ArkTS 严格模式 `arkts-no-untyped-obj-literals` 不允许内联 `{ id: 'xxx' }` 传给类型化参数。
- **修复**：每个文件顶部定义 `interface DetailNavParam { id: string }`，构造实例后传入 `pathStack.pushPath({ name: 'X', param: param })`。
- **教训**：ArkTS 严格模式比 TS 严格很多，**所有 object literal 都要有显式接口/类**，即使临时对象也不行。给 NavDestination param 一律用 `DetailNavParam` 接口。

### 类别 3：NavPathStack API 误用（1 处）
我把 `popToName(name, animate?)` 错写成 `popTo({ name: 'MainTabs', animated: false })`。
- **修复**：GlobalBottomBar.switchToTab 改用 `pathStack.popToName('MainTabs', false)`。
- **教训**：NavPathStack 的正确 API 是 `popToName(name)` / `popToIndex(index)` / `pop()` / `pushPath(info)` / `replacePath(info)` / `removeByName(name)` / `clear()` / `size()`。`popTo({ name })` 不存在。

### 类别 4：@Builder 不允许局部变量声明（4 处）
ArkUI `@Builder` 函数体只能用 UI 组件语法，不能写 `const id = ...` 局部变量。
- **修复**：RootShellPage.DestinationBuilder 内每个分支不再声明 `const id = ...`，直接把 `(param as DetailNavParam).id ?? ''` 作为组件 prop 值传进去。
- **教训**：@Builder 是 UI DSL，不是普通函数。条件分支里只能写 UI 组件或调用其他 @Builder，不能写表达式求值 / 局部变量 / 函数调用语句。复杂参数在外部 cast 好再传。

### 类别 5：onClick 返回类型不匹配（1 处）
`SearchPage` 的 `.onClick((): void => this.pathStack.pop())` 中 `pop()` 返回 `NavPathInfo`，赋给 `void` 不行。
- **修复**：改为 `.onClick((): void => { this.pathStack.pop(); })`（用代码块包装，丢弃返回值）。
- **教训**：ArkUI 回调声明 `(): void` 时，箭头函数体如果直接是返回值的表达式会失败，必须用 `{ stmt; }` 块。

### 类别 6：main_pages.json 注册残留（6 个）
main_pages.json 中注册的页面必须有 `@Entry` 装饰器。6 个 Content 文件已经迁出 `@Entry`，但仍注册在路由表里。
- **修复**：从 main_pages.json 移除 `pages/MainPage / ArtistDetailPage / AlbumDetailPage / PlaylistDetailPage / CollectionDetailPage / SearchPage`。保留 RootShellPage + 独立 @Entry 页面。
- **教训**：把 `@Entry` 改成 `@Component` 后**必须同步移除** main_pages.json 注册，否则 ArkTS 编译报错"A page configured in main_pages.json must have one and only one @Entry"。

### 类别 7：Mi cast API 错位（11 个，预先存在的 bug）
之前我重写 ArtistDetailPage 时**臆造了** `miApi.status() / qr() / devices() / poll() / cast()`，但 `features/network/api/mi.ets` 真实 API 是 `getAuthStatus / getQRCode / getQRCodeStatus / getDevices / playByUrl / playPlaylist`。
- **修复**：重写 `handleOpenMiCast / startMiPolling / castToDevice` 用真实 API。MiTrackPayload 字段对齐 `url / title / duration`（不是 id/name/artist/cover/quality）。TrackDto 字段是 `path` 不是 `url`。`getInternalQuality()` 无参。
- **教训**：**写代码前必须先 Read 真实类型定义文件**（features/network/src/main/ets/api/*.ets），不要凭印象写。这次错误完全是因为我没读 mi.ets / track.ets 就开始改 ArtistDetailPage。

### 类别 8：AppIconName 非法值（4 个，预先存在）
`AppIcon.ets` 的 `AppIconName` 类型不包含 `'upload' / 'cast'`。
- **修复**：`'upload'` → `'cloud-download'`，`'cast'` → `'volume-high'`。
- **教训**：选 AppIconName 时先查 `components/AppIcon.ets` 的类型定义。

### 修复后验证（grep 静态检查）
- 0 处 `param: { id }` 内联字面量残留
- 0 处 `this.id` 在 Content 组件内残留
- 0 处 `miApi.status/qr/devices/poll/cast` 旧 API 残留
- 0 处 `'upload'/'cast'` 非法 icon 名
- 6 个 Content 组件全部 `export struct ... Content`
- RootShellPage 注入处 entityId + DetailNavParam 都对

### 总教训
1. **改前先读类型定义**：所有 API 调用、prop 命名、对象字面量字段都要以真实类型定义文件为准，不要凭印象写。
2. **ArkTS 严格模式比 TS 严**：内联 object literal / 任意 any / 隐式 any 都报错。
3. **ArkUI 自定义组件 prop 名避开 ArkUI 保留属性**：id / width / height / onClick 等。
4. **@Builder 是 UI DSL**：不能写局部变量、复杂表达式、函数调用语句，只能写 UI 组件。
5. **NavPathStack API**：`popToName / popToIndex / pop / pushPath / replacePath / removeByName / clear / size`。没有 `popTo({ name })`。
6. **改 @Entry → @Component 必须同步 main_pages.json**：从路由表移除。
### RootShellPage 白屏/tab 失效修复（用户报告 bug）
- **根因**：`RootShellPage.ets` 的 `DestinationBuilder` 各分支直接返回 `MainPageContent`/`ArtistDetailContent` 等业务组件，**未包裹 `NavDestination()`**。ArkUI Navigation 的 navDestination 映射要求每个分支必须以 NavDestination 为根节点，否则子页面无法挂载 → 只剩平级 GlobalBottomBar，内容区全白屏。
- **修复**：每个分支包 `NavDestination() { ... }.hideTitleBar(true).width('100%').height('100%')`。
- **附带修复**：`GlobalBottomBar` 的 TabItem 原先图标/文字固定 `tabBarInactive`，无激活态。新增 `@StorageLink('mainTabIndex')`，当前 tab 用 `tabBarActive` 高亮。
- **确认无需改**：MiniPlayer 显隐逻辑正确（`!carModeEnabled` 才渲染 MiniPlayer；queue 为空时不显示）。
- **教训**：用 Navigation + navDestination builder 时，builder 返回值必须是 NavDestination 容器，这是 API 12+ 系统组件 Navigation 的硬性要求，不包裹不会报编译错，只会运行时白屏。

### 系统状态栏/底部小横条颜色不随主题切换修复
- **根因**：`EntryAbility.onWindowStageCreate` 中 `loadContent('pages/Index')` 是同步调用，而 `getMainWindow().then(applySystemBar)` 是异步的。页面先渲染完成，系统栏颜色设置太晚，状态栏和 home indicator 区域始终白底。
- **修复**：把 `loadContent` 移到 `await windowStage.getMainWindow()` 之后，确保先设置系统栏颜色再加载页面。
- **关键时序**：`await getMainWindow()` → `setSystemBarWindow` → `applySystemBar` → `loadContent`。

### 状态栏/底部小横条深色主题下仍白色 —— 真正根因与方案（修正前两次误诊）
- **现象**：深色主题下，顶部状态栏和底部小横条（home indicator）区域仍是白色，不随主题变黑。
- **前两次误诊**：①以为是 loadContent 与 getMainWindow 时序问题（无效）；②想开全局 setWindowLayoutFullScreen(true) 沉浸式（会影响全部 21 个 @Entry 页面，需逐页避让，高风险，已回退）。
- **真正根因**：代码从未让 App 内容延伸到系统栏区域，状态栏/小横条区域由系统渲染（白底 start_window_background），applySystemBar 改不动它。
- **正确方案（华为官方推荐·组件安全区方案）**：用 `expandSafeArea` 而非全局沉浸式。它只让组件**背景绘制延伸**到状态栏/导航栏，**子组件仍在安全区内自动避让**，零侵入、不影响其他页面。无需 setWindowLayoutFullScreen。
- **实施**：RootShellPage 根 Stack 加 `.expandSafeArea([SafeAreaType.SYSTEM],[SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM])`，背景色即延伸到系统栏区域，深色主题下变黑。
- **关键生效条件**（华为官方）：①组件边界与安全区边界重合（y≤状态栏高度，根容器贴满全屏满足）②不能用固定宽高（百分比可以）③父容器不能是滚动容器/不能 clip(true)。
- **SafeAreaType/SafeAreaEdge 是 ArkUI 全局枚举，无需 import**。
- **注意**：Navigation 组件默认自己就 expandSafeArea([SYSTEM],[TOP,BOTTOM])。其他独立 @Entry 页面（PlayerPage/SettingsPage/LoginPage 等 20 个）若要各自的系统栏区域也随主题，需各自根容器加 expandSafeArea——先验证 RootShellPage 主界面生效再决定是否推广。

### 状态栏/小横条修复 —— 第二次验证与补齐（用户反馈：顶部黑了、底部仍白、切后台闪白）
- **关键教训**：用户说"主界面没生效"，排查发现 **DevEco 增量编译没触发**——GlobalBottomBar 编译产物停在 04:11，源文件 04:36。代码改动对但旧产物没更新。**改鸿蒙代码后必须确认编译产物时间戳 > 源文件，否则让用户重新构建。**
- **命令行构建方式**（DevEco 不自动编译时兜底）：
  `cd apps/harmony && DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk node /Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw.js --mode module -p product=default -p module=entry assembleHap`
- **三个症状分别对应**：
  1. 底部小横条白 → GlobalBottomBar 自身背景没延伸到底部。修：根 Column 加 surface 背景 + `expandSafeArea([SYSTEM],[BOTTOM])`（对齐 mobile `height:49+insets.bottom`）。注意 expandSafeArea 组件不能设固定宽高，所以背景从 Row(height:56) 移到 Column。
  2. 顶部已黑 → RootShellPage 根 Stack 的 expandSafeArea([TOP,BOTTOM]) 生效了。
  3. 切后台闪白 → 任务卡片快照用窗口底色。修：EntryAbility 加 `onForeground()` 回前台重新 applySystemBar（内含 setWindowBackgroundColor），确保窗口底色跟随主题。

### 状态栏下方白色条带修复（第三轮：顶部系统栏黑、但其下 App 区域一条白、呼后台可见）
- **现象**：深色主题下系统状态栏（时间电量行）黑色正常，但其正下方 App 内容区顶部有一条白色，呼出后台时整条白可见。
- **根因（层级透明穿透）**：层级 Stack(黑,expand)→Navigation(黑,无expand)→NavDestination(**无背景色,默认expandSafeArea但背景透明**)→MainPageContent(黑)。NavDestination 默认 extendSafeArea 到状态栏但背景透明，盖住了根 Stack 延伸的黑背景，透出窗口白底 → 白条。
- **修复**：给 `Navigation` 加 `expandSafeArea([SYSTEM],[TOP,BOTTOM])`，让 Navigation 自身的黑背景延伸到状态栏，填在 NavDestination 透明层之下。
- **教训**：NavDestination 默认 expandSafeArea 但**背景透明**，多层容器叠用时每一层都可能透穿。expandSafeArea 只延伸"该组件自己的绘制区"，下层组件的背景延伸不会自动盖过上层透明组件。排查白色条带要逐层看哪个容器没背景色/没 extendSafeArea。
- 曾误改 MainPageContent 加 expandSafeArea[TOP]，已撤销（改在 Navigation 层更准确，一处覆盖所有 NavDestination 页面）。

### 状态栏白条 —— 真正命中根因（第四轮，官方文档佐证）
- **前三次误诊**：依次在根Stack、Navigation 上加 expandSafeArea，都没解决。因为**最上层盖住状态栏区域的是 NavDestination 自己**。
- **决定性证据（华为官方沉浸式推荐写法）**：`NavDestination() { ... }.hideTitleBar(true).backgroundColor(...)` —— NavDestination **必须显式设 backgroundColor**，否则它 extendSafeArea 到状态栏的那部分背景是默认白/透明，透出窗口白底 → 状态栏下方白条。
- **修复**：给全部 6 个 NavDestination 分支都加 `.backgroundColor(this.theme.colors.background).expandSafeArea([SYSTEM],[TOP,BOTTOM])`。
- **核心教训**：Navigation 体系里，状态栏/导航栏区域**最终由 NavDestination 这一层绘制**。根 Stack/Navigation 加 expandSafeArea 只是底层铺垫，NavDestination 不设背景色就会把它盖掉。改沉浸式背景要落在 NavDestination 层。

### PlayerPage 横屏布局 + 首页两处修复（对齐 mobile）
- **PlayerPage 横屏**：原来只有竖屏。新增 `isLandscape`（根 Column `onAreaChange` 测 width>height）分支 `landscapeContent()`：左列 Stack = 封面 250×250 居中 + 返回/更多按钮 `position` 绝对定位（左上/右上）+ 悬浮控制条（复用竖屏 `controls()`，`position y = controlsTop()`，bottom:40/left:10%/width:80%）；右列常驻 `lyricsView()`（`contentType === 'AUDIOBOOK'` 时用 `playlistPanel()`）。`needsAutoHide() = isLandscape && 250+controlsHeight+80 > containerHeight`，满足时 3s 无操作 `animateTo` 淡出控制条，点封面切换显隐。
- **关键坑 1**：8 个 bindSheet 锚点从内容 Column 内挪到 Stack 层——竖屏/横屏 if/else 整段重建会销毁锚点，导致打开中的半模态消失。
- **关键坑 2**：更多按钮贴右边缘不能用 `position x:'100%' + translate`（受 padding 影响），改为测左列宽 `leftColumnWidth`，`position x = leftColumnWidth - 64`。
- **横屏歌词交互隔离**：`lyricsView()` 点按切回封面的 onClick 加 `if (!this.isLandscape)` 守卫；歌词滚动订阅条件从 `showLyrics` 改为 `isLandscape || showLyrics`。
- **MainPage 调整顺序按钮遮挡**：GlobalBottomBar（MiniPlayer 60 + Tab 56 = 116）悬浮叠加在内容上（RootShellPage Stack alignContent: Bottom），首页 List 末尾加 116 高的空白 ListItem 避让（对齐 mobile scrollContent paddingBottom: 100）。
- **专辑骨架屏艺术家行居中**：HomeTab LoadingSkeleton 版块 3 卡片 Column 加 `.alignItems(HorizontalAlign.Start)`。

### 艺术家详情页列表全空修复（传 id 当 name 的坑）
- **现象**：鸿蒙端艺术家详情页专辑列表、合作专辑、单曲列表全空，只显示"所有单曲 (0)"。
- **根因**：后端 byArtist 系列接口按艺人**名字**查（`prisma.album.findMany({ where: { artist } })`，track 用 `contains` 模糊匹配），harmony ArtistDetailPage.load() 却把 `entityId`（艺术家 ID）直接传给 albumApi.byArtist / collaborativeByArtist / trackApi.byArtist / mvApi.byArtist，数字 ID 匹配不到任何记录。
- **修复**：对齐 mobile artist/[id].tsx 的 artistQueryKey 逻辑——`sourceType === 'emby' ? id : artist.name`，拿到 artist 详情后再用 name 并行拉关联数据。
- **教训**：harmony 端接口注释里 track.ets 已写明"按艺人「名字」查，不是 id"，但调用方没遵守。涉及 byArtist 类接口时永远先看后端 where 条件匹配的是什么字段。

### NavDestination 各页底部避让 GlobalBottomBar 统一修复
- **背景**：GlobalBottomBar（MiniPlayer 60 + Tab 栏 56 = 116）在 RootShellPage 根 Stack 里 `alignContent: Bottom` 悬浮叠加，不占布局空间，所有 NavDestination 子页滚动容器都需自行预留底部 116 的避让。
- **修复**：ArtistDetailPage（bottom: 40→116）、AlbumDetailPage（bottom: 40→116）、PlaylistDetailPage（新增 bottom: 116）、CollectionDetailPage（新增 bottom: 116）、SearchPage（结果 List + 建议 Scroll 两处都加 bottom: 116）；MainPage 首页昨天用的 116 高空白 ListItem 统一改成 List `.padding({ bottom: 116 })`，项目内写法一致。
- **约定**：以后新增 NavDestination 页面，滚动容器一律加 `.padding({ bottom: 116 })` 并注明是避让 GlobalBottomBar；若 MiniPlayer/Tab 栏高度变化需全局调整。

### PlaylistDetailPage 播放全部按钮暗色白底白字 + 右溢出修复
- **问题 1（暗色不可见）**：按钮背景是 `theme.colors.primary`（暗色模式 = 白色），文字/图标硬编码 `#FFFFFF` → 白底白字。主题系统里正确的配对色是 `theme.colors.onPrimary`（亮=白、暗=黑，与 mobile 的 colors.background 等价）。全局约定：**primary 背景上的文字/图标一律用 onPrimary，禁止硬编码 #FFFFFF**。
- **问题 2（右侧溢出屏幕）**：ArkUI 的 ListItem 里 `width('100%')` + `margin({left:16,right:16})` 不会从宽度中扣除 margin（100% 参照 List 内容区总宽），按钮向右溢出。修法：去掉 width('100%')（Row 默认拉伸自适应），左右间距改为 ListItem 的 `.padding({left:16,right:16,...})`。
- 同页 rename sheet 的确认按钮、错误态重试按钮同类问题一并换成 onPrimary。
- 存量隐患（未修，未实测影响）：MemberDetailPage:204、SettingsPage:917、PlayerPage:1167/1193、TtsConfigPage:404、LibraryPage:1138、MemberBenefitsPage:356 都有 primary 背景 + 硬编码白字的写法，暗色模式下可能同样不可见。

### 更正：ListItem 内通栏按钮的正确写法
- 上一条把播放按钮的 width('100%') 去掉是错的——ArkUI Row 默认收缩成内容宽度，按钮变成只有文字宽。
- 正确写法：`width('100%')` 保留在按钮上，左右间距放外层 ListItem 的 `.padding({left,right})`。即「宽度占满靠 100%，留白靠父级 padding，不用 margin」。

### 多选框 emoji → AppIcon + 去掉点歌自动打开播放页
- **多选状态**：ArtistDetailPage / AlbumDetailPage / LibraryPage 三处的 ☑/☐ emoji 换成 AppIcon 的 'checkbox' / 'square-outline'（系统符号 checkmark_square_fill / square，已在 AppIcon.ets 注册），对齐 mobile 的 Ionicons 方案。选中色 primary、未选中 textSecondary。
- **点歌不再自动打开 PlayerPage**：删除 7 处 playTrackList 后的 `router.pushUrl('pages/PlayerPage')`——ArtistDetailPage、AlbumDetailPage、LibraryPage、PlaylistDetailPage（handlePlayAll + playFromIndex 两处）、MainPage(HomeTab)、SearchPage、PersonalPage。保留的入口：MiniPlayer 点击、EmbeddedPlayerPanel（车机）、MVDetailPage 等原本就是"显式打开播放器"的场景。PlaylistDetailPage 的 router import 已无引用，一并删除。
- **行为对齐**：现在点任意单曲只开始播放（MiniPlayer 出现），进播放详情页需手动点 MiniPlayer——与主流音乐 App 一致。

### Tab 栏背景色统一 + 底部/内部 tab 切换不再闪骨架屏
- **GlobalBottomBar 背景色**：`surface`（灰）→ `tabBar`（亮=白/暗=黑），与 MiniPlayer 的 `background` 一致，对齐 mobile 的 colors.tabBar。
- **底部三 tab 常驻**：MainPageContent 常规布局和车机 carContentPanel 从 `if (currentIndex===N)` 改成 Stack 三层常驻 + `.visibility()` 切换显隐。原来每次切 tab 整棵子树销毁重建 → aboutToAppear 重拉数据 → 骨架屏；现在状态/滚动位置/数据全保留。注意：三个 tab 首次进入时会同时挂载并各自拉一次首屏数据（可接受的预加载）。
- **声仓内部 tab 缓存**：LibraryTab 加 `tabDataLoaded` 缓存（key=`${tab}_${mode}`），loadTabData 成功后标记；switchTab 只在无缓存时拉取。下拉刷新仍走 `loadTabData(true)` silent 强制更新；切 MUSIC/AUDIOBOOK 模式时因 key 含 mode 缓存自然失效。
